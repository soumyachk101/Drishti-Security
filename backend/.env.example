# Railway sets PORT automatically — no need to set this locally
# PORT=8000

# Optional: enable Claude-powered AI features
# Without this, remediation/summaries/kill-chains use sensible fallbacks
ANTHROPIC_API_KEY=sk-ant-...
